[project]
name = "kaggriculture"
version = "0.1.0"
description = "A deterministic market-aware agent for the Kaggriculture simulation."
requires-python = ">=3.12,<3.13"

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
pythonpath = ["."]
